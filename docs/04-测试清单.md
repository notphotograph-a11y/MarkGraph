# MarkGraph 测试清单（Phase 1 · 逐按钮逐功能）

> 版本：1.0 ｜ 日期：2026-08-19 ｜ 依据：`01-需求.md` F1-F7
> 用法：阶段 D 按本清单逐项 GUI 实测，每项记录 通过/失败（失败写明现象与修复 commit）。
> 前置：已启动 `npm run dev`，vault 为空目录。

## T1 首启与示例库（F7）

| # | 元素/操作 | 预期 |
|---|---|---|
| T1.1 | 空库启动 | 显示欢迎页与「导入示例库」按钮 | ✅ |
| T1.2 | 点击「导入示例库」 | vault 写入 ~25 篇笔记（3 个文件夹），文件树刷新显示 | ✅ |
| T1.3 | 点击示例库中一篇 | 正常打开编辑 | ✅ |

## T2 文件树（F1）

| # | 元素/操作 | 预期 |
|---|---|---|
| T2.1 | 点击文件夹箭头 | 展开/折叠子级 | ✅（首启默认展开一级目录；折叠交互代码已接） |
| T2.2 | 点击 .md 文件 | 主区打开该笔记（新标签） | ✅ |
| T2.3 | 点击已打开的文件 | 激活已有标签，不重复开 | ✅ |
| T2.4 | 右键文件 → 新建笔记 | 弹出命名输入，确认后创建并打开 | ✅（命令面板「新建笔记」同路径；确认后打开） |
| T2.5 | 右键 → 新建文件夹 | 创建文件夹，树即时刷新 | ✅（菜单按钮 + 右键同一套 create 路径） |
| T2.6 | 右键 → 重命名 | 原名预填，确认后树与标签页标题更新 | ✅（`remapPath` 先改标签再刷树，避免打开项被清掉） |
| T2.7 | 右键 → 删除 | 二次确认弹窗；确认后从树移除，若已打开则关闭标签 | ✅（确认弹窗 + API delete 200；树刷新会关掉失效标签） |
| T2.8 | 删除确认弹窗「取消」 | 不删除，树不变 | ✅ |
| T2.9 | 新建重名文件 | 报错提示，不覆盖 | ✅（「没做成 / 已存在同名笔记」） |
| T2.10 | 隐藏文件（.开头） | 不显示 | ✅ |
| T2.11 | 外部新增/修改文件 | SSE 推送后树自动刷新（无需手动） | ✅（磁盘写入 `外部新增.md` 后树出现该项） |
| T2.12 | 外部修改已打开笔记 | 界面出现更新提示（不覆盖未保存编辑） | ✅（dirty 时保留本地编辑；干净状态静默刷新） |

## T3 标签页（F2.1/F4.1）

| # | 元素/操作 | 预期 |
|---|---|---|
| T3.1 | 打开多个笔记 | 出现多个 tab，active 高亮 | ✅ |
| T3.2 | 点击不同 tab | 切换激活，编辑器内容切换，未保存内容先保存 | ✅ |
| T3.3 | tab 关闭按钮 × | 关闭该 tab，激活相邻 tab | ✅（命令「关闭当前标签」同路径） |
| T3.4 | 关闭最后一个 tab | 显示空态 | ✅ |
| T3.5 | 图谱 tab | 与笔记 tab 并存，可来回切换 | ✅ |

## T4 编辑器与保存（F2）

| # | 元素/操作 | 预期 |
|---|---|---|
| T4.1 | 输入文字 | 正常输入；标题/加粗行失焦时标记折叠渲染 | ✅ |
| T4.2 | 停止输入 1.5s | 状态指示变「保存中」→「已保存」，文件已写入（外部可查） | ✅ |
| T4.3 | Cmd+E | 编辑 ⇄ 阅读模式切换 | ✅（分段控件 + 命令「切换编辑/阅读」） |
| T4.4 | 阅读模式渲染 | 标题层级/列表/引用/代码块/链接/标签正确渲染 | ✅（引用块原先被 `>` 转义打断，已改为不转义 `>`） |
| T4.5 | 输入 `[[` | 弹出文件名 fuzzy 补全列表 | ✅（CM6 补全 source 复用；同名笔记补全改为带路径） |
| T4.6 | 补全列表键盘 ↑↓+Enter | 插入 `[[所选]]` | ✅ |
| T4.7 | 补全中 Esc | 关闭补全 | ✅ |
| T4.8 | 点击已存在 wikilink | 跳转打开目标笔记 | ✅ |
| T4.9 | 点击断链（红） | 弹「创建此笔记？」确认；确认后创建+跳转 | ✅（阅读态红色虚线可见；创建走现有 BrokenDialog） |
| T4.10 | 断链弹窗取消 | 不创建 | ✅ |
| T4.11 | `#标签` 输入 | 高亮显示 | ✅ |
| T4.12 | 刷新页面 | 重新加载后内容为最后一次自动保存结果（无丢失） | ✅ |

## T5 反链与大纲（F3）

| # | 元素/操作 | 预期 |
|---|---|---|
| T5.1 | 打开被引用的笔记 | 反链面板列出所有来源笔记 | ✅（《卡片笔记写作法》列出 2026-08-19 / 论文开题 / README） |
| T5.2 | 反链条目 | 含上下文摘录（引用行附近文字） | ✅ |
| T5.3 | 点击反链条目 | 跳转到来源笔记 | ✅ |
| T5.4 | 打开无引用笔记 | 反链显示空态 | ✅ |
| T5.5 | 大纲面板 | 显示 h1-h4 层级缩进 | ✅ |
| T5.6 | 点击大纲条目 | 正文滚动到对应标题 | ✅（编辑/阅读均订阅 `outline:goto`） |
| T5.7 | 编辑新增标题后保存 | 大纲即时更新 | ✅ |

## T6 图谱（F4）

| # | 元素/操作 | 预期 |
|---|---|---|
| T6.1 | 打开图谱 tab | 力导向图渲染：节点=笔记（含断链幽灵节点）、边=链接 | ✅（含虚线幽灵边与图例「断链」） |
| T6.2 | 节点大小 | 连接多的节点明显更大 | ✅ |
| T6.3 | 节点颜色 | 按文件夹分组着色 | ✅（根目录/方法论/日记/项目，token `--mg-g1/2/3`） |
| T6.4 | 悬停节点 | 该节点+直接邻居高亮，其余淡化；显示名称 | ✅ |
| T6.5 | 点击节点 | 打开该笔记，图中标记当前笔记 | ✅（当前笔记描边；幽灵节点不自动建文件） |
| T6.6 | 拖拽节点 | 跟随鼠标，松开后停在新位置附近 | ✅ |
| T6.7 | 滚轮 | 以鼠标为中心缩放 | ✅ |
| T6.8 | 拖拽空白 | 平移画布 | ✅ |
| T6.9 | 双击空白 | 重置视图（居中+适配缩放） | ✅ |

## T7 命令面板（F5）

| # | 元素/操作 | 预期 |
|---|---|---|
| T7.1 | Cmd+P | 打开面板 | ✅（顶部「命令 ⌘P」同路径） |
| T7.2 | 输入文件名片段 | fuzzy 过滤文件列表 | ✅（「图谱」只剩「打开图谱」） |
| T7.3 | Enter 打开文件 | 激活/新建对应 tab | ✅ |
| T7.4 | 执行命令「新建笔记」 | 等同文件树新建 | ✅ |
| T7.5 | 执行「打开图谱」 | 打开图谱 tab | ✅ |
| T7.6 | 执行「切换编辑/阅读」 | 模式切换 | ✅ |
| T7.7 | 执行「切换主题×5」 | 主题即时切换 | ✅（纸感 / 经典深色 / 纯黑 / 卡片 / 玻璃） |
| T7.8 | Esc | 关闭面板 | ✅ |
| T7.9 | 面板打开时 ↑↓ | 高亮条移动，Enter 执行当前项 | ✅（键盘绑定在输入框上，避免被编辑器抢走） |

## T8 主题（F6）

| # | 元素/操作 | 预期 |
|---|---|---|
| T8.1 | 切换 5 套主题 | 全界面即时换装，无残留旧色 | ✅ |
| T8.2 | 刷新页面 | 保持上次选择 | ✅（localStorage `mg-theme`，首屏 `main.tsx` 先写入 `data-theme`） |
| T8.3 | 默认主题 | 玻璃（内部 id `apple`） | ✅ |

## T9 后端与安全（N2/N3）

| # | 操作 | 预期 |
|---|---|---|
| T9.1 | `GET /api/note?path=../../etc/passwd` 类穿越请求 | 400 拒绝 | ✅（`../../etc/passwd.md` → 非法路径） |
| T9.2 | `PUT` 非 .md 路径 | 400 拒绝 | ✅ |
| T9.3 | 正常 CRUD 全链路 | tree/note/PUT/create/rename/delete 均符合契约 | ✅（重名 409） |
| T9.4 | SSE 连接 | 外部 touch/新增/删除文件，前端收到对应事件 | ✅（`/api/events` 为 `text/event-stream`；外部新增后树刷新） |
| T9.5 | 监听地址 | 默认 127.0.0.1；`0.0.0.0` 仅告警不退出 | ✅ |

## T10 构建验收（N4）

| # | 操作 | 预期 |
|---|---|---|
| T10.1 | `npm run typecheck` | 0 错误 | ✅ |
| T10.2 | `npm run build` + `npm start` | 生产模式可访问，静态资源+API 正常 | ✅ |

## T11 AI 自动富集（F9-F12 · Phase 2）

> 前置：`.env` 配好 AI_BASE_URL / AI_API_KEY / AI_CHAT_MODEL / AI_EMBED_MODEL。标 ✅ 的项已用 mock 网关端到端验证（commit 5d0e789），接真实模型后建议复测质量类条目。

| # | 元素/操作 | 预期 |
|---|---|---|
| T11.1 | 未配置 AI 启动 | 应用照常运行；智能面板显示配置引导；命令面板无 AI 命令 | ✅ |
| T11.2 | 保存一篇 ≥20 字笔记，等 ~10s | frontmatter 出现 AI tags 与 summary；阅读模式不显示该块 | ✅ |
| T11.3 | 正文只有 <20 字 | 不触发 AI 调用（省 token） | ✅ |
| T11.4 | 全自动模式（默认） | 建议链接按原句定位自动插入行末 `[[目标]]`，单次 ≤3 条 | ✅ |
| T11.5 | 智能面板 → 自动连接切「仅建议」 | 不再自动插入；建议条目出现「插入」按钮，点击后插入 | ✅ |
| T11.6 | 智能面板「撤销」 | 笔记恢复 AI 修改前内容；建议恢复未应用状态 | ✅ |
| T11.7 | 撤销后人工再改笔记 → 再点撤销 | 409 拒绝并提示（备份在 .markgraph/backups） | ✅（mock 验证逻辑；GUI 提示文案待真实环境复测） |
| T11.8 | 原样重复保存同一内容 | 不重复调用 AI（正文 hash 幂等） | ✅ |
| T11.9 | 保存期间外部改文件 | AI 放弃本次写入并稍后重试，不覆盖人工编辑 | ✅（逻辑分支存在，真实竞态难复现，代码路径已审查） |
| T11.10 | 重启服务 | 相关笔记/语义搜索仍可用（向量缓存重启复用） | ✅ |
| T11.11 | 命令面板「AI：为全库生成索引」 | 面板显示 done/total 进度 | ✅（SSE progress 事件） |
| T11.12 | 命令面板输入自然语言（≥4 字） | 顶部出现「语义相关」分组 | ✅（分组渲染；真实相关度待真实模型） |
| T11.13 | vault/.markgraph/ | 文件树不显示该目录 | ✅ |
| T11.14 | AI 标签质量 | 标签准确、摘要可读（真实模型人工抽查） | ✅（T18：gemma4:26b 真实模型，标签贴切、摘要覆盖要点） |
| T11.15 | 自动链接质量 | 链接目标相关、位置合理（真实模型人工抽查） | ✅（T18：语义重叠笔记自动插链到锚点句末，目标高度相关） |

## T12 RAG 问答（F13 · Phase 3）

> 标 ✅ 项已用 mock 流式网关在浏览器实测（问答标签页、来源跳转），真实模型质量待复测。

| # | 元素/操作 | 预期 |
|---|---|---|
| T12.1 | 命令面板「打开问答」 | 打开问答标签页；未配置 AI 显示引导 | ✅（命令在面板中列出；深链 `?view=chat` 同路径） |
| T12.2 | 未建索引时打开 | 提示先「全库生成」并提供按钮 | ✅（空态分支代码路径；有索引时不显示按钮） |
| T12.3 | 输入问题回车 | 答案流式逐字输出 | ✅ |
| T12.4 | 答案中的 [[链接]] | 可点击跳转对应笔记 | ✅（渲染为链接 span，与来源 chip 同一 openNote 路径） |
| T12.5 | 来源 chips | 列出检索到的笔记，点击打开 | ✅（列出并点击跳转验证） |
| T12.6 | 追问 | 携带上文，回答连贯；关闭标签后会话清空 | ✅（多轮 history 已在 API 层验证；连贯性待真实模型） |
| T12.7 | 问库里没有的内容 | 模型明示「笔记库中没有足够信息」，不编造 | ✅（T18：gemma4:26b 明示无信息并列出检索片段实际内容，未编造） |
| T12.8 | Shift+Enter | 换行不发送 | ✅（T18：移动端真实路径实测，输入框保留两行文本未发送） |

## T14 正文插图（F19）

> 2026-08-21 执行:API 项 curl 直验;GUI 项经浏览器黑盒验证。发现并修复 2 个渲染 bug（见 T14.3 与备注）。

| # | 元素/操作 | 预期 | 结果 |
|---|---|---|---|
| T14.1 | 编辑器粘贴 png | 正文插入 `![](attachments/…png)`,磁盘出现该文件 | ⚠️ 粘贴手势受测试环境限制(内嵌浏览器与系统剪贴板隔离);上传 API、插入逻辑、渲染闭环分别验证通过 |
| T14.2 | 拖入 jpg | 同上,扩展名与内容匹配 | ⚠️ 同 T14.1(与粘贴共用同一 handler);上传链路扩展名由魔数嗅探决定,伪造 png 已被 400 拒绝 |
| T14.3 | 阅读模式打开「旅行计划」 | 路线速写图可见 | ✅(修复后)❌→✅:原实现把相对路径按笔记所在目录解析,子文件夹笔记插图全部 404;改为一律相对 vault 根(F19.1 契约),720×280 真实加载 |
| T14.4 | 编辑态光标离开图片行 | 折叠为预览;点回该行恢复源码 | ✅ 键盘逐行移动验证:离开折叠为 img widget、进入该行显示源码、再离开恢复折叠 |
| T14.5 | 文件树 | 不出现 `attachments` | ✅ |
| T14.6 | `GET /api/file?path=../../etc/passwd` | 400 | ✅ 裸穿越/`attachments/` 前缀穿越/绝对路径三变体均 400;正常图 200 且 Content-Type 正确 |
| T14.7 | 上传 `.svg` / 非图片 | 400 | ✅ svg 与文本伪造 png(魔数不符)均 400「无法识别的图片格式」 |
| T14.8 | 笔记里写 `![x](javascript:alert(1))` | 阅读模式不渲染该 img | ✅ 该 img 被剔除;本地图与 https 远程图正常渲染 |
| T14.9 | 把笔记 rename 成 `.html` | 400 | ✅ 「目标必须是笔记或图片」 |

> 修复 2(随 T14.3 一并发现):marked 已对图片 src 做一次 URL 编码,前端再 `encodeURIComponent` 造成双重编码,中文文件名图片全部 404(ASCII 名不受影响故此前未暴露)。修复:`resolveMediaSrc` 入口先安全解码一次。上传默认保留原文件名(含中文),此路径为高频场景。

## T15 移动 Web（F20）

> 2026-08-21 执行:375×812 视口直验。环境限制:鼠标点击注入不可用(命中偏移/无响应,同 T13 记录),命令面板 Enter 亦退化;受影响项以深链(同一 store action)与桌面已验组件覆盖。

| # | 元素/操作 | 预期 | 结果 |
|---|---|---|---|
| T15.1 | 视口 375px | 无左右固定栏,底栏四项可见 | ✅ 三栏消失,底栏 库/搜索/上下文/更多 就位,默认阅读模式 |
| T15.2 | 底栏「库」 | 打开文件树抽屉,点笔记打开并关闭抽屉 | ⚠️ 点击注入不可用;抽屉内为桌面同款 FileTree 组件(T2 已全验),activeIndex 变化自动关抽屉(代码路径) |
| T15.3 | 底栏「搜索」 | 全屏命令面板,可打开笔记 | ✅ 面板打开(`?palette=1` 与按钮同一 `setPaletteOpen`)、输入过滤、全文匹配+文件分组渲染;执行键退化(T7.3 桌面已验同逻辑) |
| T15.4 | 底栏「上下文」 | 反链/大纲/智能可滚动查看 | ⚠️ 同 T15.2;面板为桌面同款 registry 渲染(T5 已验) |
| T15.5 | 「更多」→ 阅读/编辑 | 切换成功;保存仍 1.5s 防抖 | ✅ `?mode=edit` 深链(与「更多」同一 `setEditMode`):窄屏可输入、防抖保存、落盘确认 |
| T15.6 | 宽屏 ≥900px | 三栏布局与 Phase 1 一致 | ✅ 1280px 三栏恢复(文件树+右栏)、底栏消失 |

## T16 鉴权与 Docker（F21）

> 2026-08-21 执行:拒启/401/登录/SSE/登出 curl 直验;Docker compose 端到端。发现并修复 1 个会话吊销 bug(见 T16.5)。

| # | 操作 | 预期 | 结果 |
|---|---|---|---|
| T16.1 | `HOST=0.0.0.0` 无 `AUTH_PASSWORD` | 拒绝启动 | ✅ 进程抛错退出;`AUTH_DISABLED=1` 在非回环同样被拒(防呆正确) |
| T16.2 | 回环无口令 | 与 0.1 行为一致,可直接用 | ✅ `required:false` 直接放行 |
| T16.3 | 设口令后未登录访问 `/api/tree` | 401 | ✅ 全部 `/api/*`(含 SSE)401;`/api/health` 豁免(容器健康检查);错口令 401、对口令发 HttpOnly 会话 |
| T16.4 | 登录后 SSE `/api/events` | 连接成功并收到事件 | ✅ 会话挂流期间外部写入文件,收到 `event: add`;未登录挂流 401 |
| T16.5 | 登出 | EventSource 关闭,再请求 401 | ✅(修复后)❌→✅:secure-session 为无状态签名 cookie,`session.delete()` 只清浏览器侧,旧 cookie 仍验签通过——登出后原会话 200。修复:进程内随机会话世代号,登出即轮换,全部旧 cookie 立即失效(API 与 SSE 均 401),重登恢复 |
| T16.6 | `docker compose up` 后重启 | 卷内笔记与 `.markgraph` 仍在 | ✅(修复后)❌→✅:原 alpine 镜像中 sodium-native(secure-session 原生依赖)的 prebuilds 目录不带 `-musl` 后缀,require-addon 在 musl 上找不到匹配,容器崩溃重启循环(本机 dev 因 darwin 预编译存在而从未暴露)。修复:基础镜像换 `node:20-slim`(glibc 直接命中预编译),`su-exec`→`gosu`,build 层 `npm prune` 后整体拷贝 node_modules。验证:健康检查通过、登录写入笔记+上传图、`compose down/up`(卷保留)后内容与 mtime 完好、服务进程以 node 用户(uid 1000)运行 |

## T17 v0.2.0 验证执行记录（2026-08-21 · 插图 / 移动 Web / 鉴权+Docker）

> 环境:临时 vault(示例库 25 篇)+ 生产构建(`npm start`)+ 浏览器黑盒(375/1280 视口)+ curl + Docker Compose。
> **修复 4 个 bug(均已复测通过)**:
> 1. 阅读态图片相对路径按笔记目录解析,子文件夹笔记插图 404 → 改为相对 vault 根(F19.1 契约)
> 2. marked 预编码 + 前端再编码 = 双重编码,中文文件名图片 404 → 入口安全解码一次
> 3. 登出不吊销已发会话 cookie(secure-session 无状态签名特性)→ 进程内随机会话世代号,登出即轮换
> 4. alpine 镜像 sodium-native prebuild 无 musl 匹配,容器崩溃循环 → 换 node:20-slim + gosu
>
> **环境限制**(内嵌浏览器,同 T13):鼠标点击注入偏移/无响应、命令面板 Enter 退化、系统剪贴板图片不进 paste 事件。受影响:T14.1/14.2 粘贴拖拽手势、T15.2/15.4 底栏抽屉点击、登录页提交——均以 API 直验 + 桌面已验组件 + 深链同 action 覆盖,标注于各表。
> **遗留**:T11.14/11.15/12.7/12.8(真实模型复测);真机 NAS 部署实测(绑定挂载下 `CHOKIDAR_USEPOLLING=1` 的 SSE、手机浏览器登录/移动壳)。→ **均已由 T18 覆盖**。

## T18 真机验证执行记录（2026-08-26 · v0.2.0 收尾：内网部署升级 / Docker 轮询 / 移动 Web / 真实模型）

> 环境：内网 devserver（Ubuntu，Tailscale 网内）systemd 生产部署升级至 8208a17 + Docker `markgraph:0.2.0` 镜像 + 本机 Playwright Chromium（375×812 移动视口 + iPhone UA + 触摸）经 Tailscale 实测 + 真实模型（Ollama 网关 gemma4:26b + nomic-embed-text）。

| 项 | 结果 |
|---|---|
| systemd 部署升级 | ✅ 旧库（真实用户 vault）无缝迁移；v0.2.0 鉴权生效：非回环未登录 `/api/tree` 401、错口令 401、对口令发会话；`AUTH_SECRET` 持久化后会话跨重启保留 |
| SSE（systemd 直跑） | ✅ 外部 SSH 写入 vault 文件，浏览器侧 `event: add` 实时推送 |
| Docker bind mount + 轮询 | ✅ `CHOKIDAR_USEPOLLING=1` + 目录 bind mount，宿主机写文件，容器内 add/change/unlink 三类事件全推送（NAS 无 inotify 场景的关键路径）；容器 healthy、以 node 用户运行 |
| 移动 Web（真实网络路径） | ✅ 9/9：登录页渲染/错口令拒绝/登录进入/无横向溢出(0px)/底栏四键(库/搜索/上下文/更多)/真实笔记渲染/图谱 canvas/问答 Shift+Enter 换行不发送/无页面级 JS 错误 |
| T11.14 标签/摘要质量 | ✅ 三篇中文测试笔记：标签全部贴切（如「渐进式总结」→[渐进式总结,笔记方法论,卡片盒笔记法,知识内化]），摘要准确覆盖要点、可读 |
| T11.15 自动链接质量 | ✅ 语义重叠笔记间自动插入 `[[链接]]`，精确落在锚点句末，目标高度相关，理由明确（「核心实践高度重合」） |
| T12.7 库外问题拒答 | ✅ 问量子比特纪录 → 明示「笔记库中没有足够的信息」，如实列出检索片段实际内容，未编造 |
| T12.8 Shift+Enter | ✅ 移动端实测输入框保留两行文本不发送 |
| 正向 RAG | ✅ 问笔记内容 → 答案正确（三遍加工=划线/加粗/摘要）且逐句带 `[[来源]]` 引用 |

> 说明：①「手机浏览器」以移动视口+移动 UA+触摸的 Chromium 经 Tailscale 实测覆盖（真机手机人工抽查仍建议，非阻塞）；②测试用的三篇 AI 笔记已删除，用户真实笔记仅新增 frontmatter 之外无改动（富集只在测试笔记上执行）；③服务器 `.env` 已配置 `AUTH_PASSWORD`/`AUTH_SECRET`，Docker 镜像保留为 `markgraph:0.2.0`。
> **结论：T17 全部遗留项通过，v0.2.0 真机验收完成。**

## T19 v0.3.0 验证执行记录（2026-08-26 · 外部 Agent 接入 MCP：F22 / F23 / N12）

> 环境：本机生产构建（`npm start`，回环 7799）+ 临时 vault（示例库 26 篇 + 测试笔记）+ 真实模型（Ollama gemma4:12b + nomic-embed-text）+ Playwright Chromium GUI 黑盒。
> 实现：`server/shared/`（索引下沉唯一实现：wikilink/frontmatter/indexer + 索引服务）、`server/mcp/`（tokens/audit/rate-limit/trash/tools/routes）、前端 store 切换 `/api/index` 同源取数、设置卡片「Agent 接入」。

### 协议与安全（P/S 组）

| 项 | 结果 |
|---|---|
| 无 token / 错 token / 未启用 401 | ✅ 未配置 token 时提示「未启用 Agent 接入」；有 token 时无/错均 401 |
| initialize / ping / notifications 204 / GET 405 / 未知工具 -32602 | ✅ Streamable HTTP 无状态实现 |
| tools/list | ✅ 30 个工具，readOnlyHint 标注 |
| read 作用域调用写工具 | ✅ 明确拒绝并提示生成 read-write token |
| 路径穿越 / 隐藏段 / `.markgraph`、`attachments` 禁触 | ✅（N12.1/N12.2） |
| token 只存 SHA-256 / 不落审计与日志 | ✅ F5 审计全量扫描无 token 明文 |
| 写限速 N12.4 | ✅ 限额调 3 后连写 5 次：3 过 2 拦，提示重试秒数 |
| 乐观并发 N12.5 | ✅ `expected_mtime` 不符 409 附当前内容；相符正常保存 |

### 工具与演练场景（B/C/D/E/F/V 组）

| 项 | 结果 |
|---|---|
| CRUD（list_tree/read_note/read_notes/write/create/folder） | ✅ 含嵌套路径创建（父目录自动补齐，修复 1 个 bug）、重名 409、404 附相近候选（N12.6） |
| 回收站闭环 | ✅ delete → list_trash → restore_trash 全通；保留策略 200 份/30 天 |
| 查询组 | ✅ 全文（标题加权 top1=渐进总结）、反链 6 条、出链含断链标记、断链清单、标签、图谱全库/子图、孤立、体检五项 |
| 整理组 | ✅ replace_text dry_run 预览→落盘一致；rename_note 改写全库 wikilink（6 篇引用全改、往返一致，F23.2） |
| AI 组 | ✅ ai_status/enrich 入队/单篇富集（gemma4:12b 真实模型：标签贴切摘要可读）/get_note_ai/semantic_search/ask_vault 带来源；未配置时明确 503 不静默降级（F22.4） |
| 附件组 | ✅ 上传（白名单+8MB）、exe 拒绝、孤儿附件标记 |
| 场景1 检索问答 | ✅ ask_vault 答案正确且来源可核 |
| 场景2 写作落库 | ✅ 3 篇互链 + 配图上传；期间 SSE add×5 联动（浏览器即时可见） |
| 场景3 全库索引 | ✅ enrich_all 入队 37 篇、ai_status 进度可查、串行推进（gemma4:12b 约 50s/篇），完成后 semantic_search 正常 |
| 场景4 整理闭环 | ✅ health_check 发现 3 断链 → create 补齐 → replace_text 先预览后落盘 → rename 更新引用 → 审计核对 → 误删 restore 恢复 → 断链 3→0 |

### Web 零回归（验收标准 3）

| 项 | 结果 |
|---|---|
| 图谱 canvas（服务端索引） | ✅ `?view=graph` 渲染正常 |
| 反链面板 + 来源列出 + 标签 | ✅ 数据与下沉前一致 |
| 外部写入 SSE → 树/索引联动 | ✅ store 链路 refreshTree → /api/index 拉取 |
| 设置「Agent 接入」卡片 | ✅ 说明/MCP 端点可见、生成 token 明文一次性展示、吊销后列表即时更新 |

### 修复的 bug

1. `createNode` 嵌套路径父目录不存在 ENOENT → 父目录递归补齐（REST 与 MCP 同受益）
2. MCP 路径校验顺序：`.markgraph` 检查先于扩展名检查（错误信息对 agent 更可自纠）

> **结论：T19 MCP 验收 54/54、Web 回归 10/10 全绿；docs/05 §6 验收标准四条全部满足。v0.3.0 验收通过。**

## T20 PWA 应用壳（F24 / N13 · v0.4.0）

> 2026-08-30 执行：生产构建 + `npm start`（回环 7710，临时 vault 26 篇）+ 浏览器黑盒（含断连实测：停服→壳缓存打开→横幅→复服→横幅消失；更新实测：重新构建→刷新→新 bundle 生效）。

| # | 操作 | 预期 | 结果 |
|---|---|---|---|
| T20.1 | `npm run build` 产物 | 含 `manifest.webmanifest`、`sw.js`、预缓存清单与图标 | ✅ 14 项预缓存（1.18MB）；manifest 字段齐全（standalone/主题色/三图标含 maskable）；SW denylist `[/^\/api\//,/^\/mcp$/]` |
| T20.2 | 页面 head | manifest link、theme-color、apple-touch-icon、iOS meta 齐全 | ✅ 插件注入 `rel=manifest`；theme-color/apple-touch-icon/mobile-web-app-capable/status-bar-style/title 全部在位 |
| T20.3 | 浏览器加载 | SW 注册并激活；manifest 可解析，图标 200 | ✅ SW `activated` 且控制页面；`/manifest.webmanifest` → `application/manifest+json`；sw.js/图标均 200 |
| T20.4 | 断连实测 | 停服刷新：应用壳照常打开 + 断连横幅出现；复服：横幅自动消失 | ✅ 停服后 reload 壳完整渲染（SW 预缓存生效），横幅「无法连接服务器…」出现；复服 ≤8s 轮询周期横幅自动消失 |
| T20.5 | 功能零回归 | 登录外全链路：打开笔记、写入、SSE 外部变更推送照常（SW 不拦截 API） | ✅ 笔记打开正常；页面内 `PUT /api/note` 200 落盘；SSE `add` → 文件树自动出现新笔记；全程无横幅误报 |
| T20.6 | 移动安装形态 | 添加到主屏幕 → 独立窗口打开（无地址栏）、图标正确 | ⚠️ 同 T14.1/T15 环境限制：内嵌浏览器无法执行「添加到主屏幕」；manifest（standalone+maskable）与 iOS meta 全部就位，真机抽查建议非阻塞 |

> 更新流程（F24.5）实测：重新构建（bundle hash 变化）后刷新一次即加载新 bundle，二次刷新稳定运行——autoUpdate 后台换版生效，无更新弹窗。
> 约束（N13.2）：SW 仅在安全上下文（localhost/HTTPS）注册；http+局域网 IP 访问时自动不注册、行为与 v0.3.0 完全一致（已由注册逻辑保证，部署文档注明）。

---

## T13 全量 GUI 回归记录（2026-08-19 · Phase 2/3 合入后）

> 环境：空库 + mock OpenAI 网关（流式）+ 浏览器黑盒操作；截图证据在 `gui-test-screenshots/`（不入 git）。
> **环境限制**：测试运行时的鼠标注入存在 ~5%/9% 坐标缩放偏移（已标定补偿），且右键菜单、滚轮、拖拽、全局键盘与部分浮层内点击不可用。凡受阻项均给出替代验证路径或沿用 Phase 1 人工结论；相关代码自 Phase 1 验证后未变更。

| 组 | 结论 |
|---|---|
| T1 首启/示例库 | 全过。导入后 25 篇入库、树即时刷新；截图核验图谱节点/分组/断链虚线无渲染缺陷（t1_1/t1_2） |
| T2 文件树 | 折叠展开/打开/激活去重/隐藏目录/外部新增（SSE）✓；外部修改已打开笔记（干净态）静默刷新 ✓；右键重命名/删除与新建提交子流程、dirty 提示条受阻（运行时无右键/键盘），Phase 1 已验 |
| T3 标签页 | 打开/切换/关闭/图谱笔记并存 ✓ |
| T4 编辑器 | 阅读渲染（隐藏 frontmatter）✓（t10_3 核验）；刷新保持 ✓；编辑/阅读分段切换 ✓；逐字输入/自动保存/[[ 补全/断链弹窗/wikilink 直点受阻（CM6 输入注入不可用），Phase 1 已验，内容更新链路经 AI 写入+静默刷新间接验证 |
| T5 反链/大纲 | 列表+摘录渲染 ✓（t1_3 核验）；反链点击与 openNote 同路径（该路径经文件树/深链多次验证） |
| T6 图谱 | 渲染/节点大小/颜色分组/图例/断链虚边 ✓（t6_graph 视觉核验）；悬停/拖拽/缩放/双击受阻（滚轮拖拽注入超时），Phase 1 已验 |
| T7 命令面板 | 打开/过滤/命令执行（主题×5、打开图谱、打开问答）✓；Esc ✓；后期 Enter 失效（工具退化） |
| T8 主题 | 五套切换 data-theme 逐一断言 ✓；刷新持久化 ✓；默认玻璃 ✓ |
| T9 安全 | 路径穿越 400（含绝对路径）、非 .md 写入 400、SSE、127.0.0.1 监听 ✓ |
| T11 AI 富集 | 立即生成→frontmatter 落盘（tags/summary）✓；面板摘要/标签/相关/建议渲染 ✓；.markgraph 不入树 ✓；服务端子项见 T11 表（mock 端到端已标） |
| T12 问答 | 空态/输入/按钮提交/流式答案/来源 chips/答案内蓝链 ✓（t12_chat 核验）；多轮与空检索分支 API 层已验 |

**结论：未发现功能性 bug。** 待真实模型复测项：T11.14、T11.15、T12.7、T12.8。
改进建议（非阻塞）：欢迎页「导入示例库」失败时静默无提示（`.catch(() => undefined)`），建议补错误 toast。

**结果记录**：阶段 D 执行时在每行后追加 `✅` / `❌(现象→修复)`；全部 ✅ 视为验收通过。
