# MarkGraph 架构与技术方案（Phase 1）

> 版本：1.0 ｜ 日期：2026-08-19 ｜ 依据：`01-需求.md`

## 1. 总体架构

```
┌────────────────────────────── 浏览器 ──────────────────────────────┐
│  React SPA（Vite 构建）                                             │
│  ├── 布局壳：文件树 | 标签页(笔记/图谱) | 右栏面板插槽                │
│  ├── CodeMirror 6 编辑器（live preview + wikilink 扩展）             │
│  ├── 图谱：d3-force 布局 + canvas 渲染                               │
│  ├── zustand store（vault 树、标签页、索引缓存、主题）                │
│  └── 事件总线 + 命令 registry                                       │
└───────────────┬───────────────────────▲───────────────────────────┘
        REST /api/*（fetch）            SSE /api/events（chokidar 监听）
┌───────────────▼───────────────────────┴───────────────────────────┐
│  Fastify (Node + TS)                                                │
│  ├── vault 文件 CRUD（fs/promises，路径校验防穿越）                  │
│  ├── chokidar 监听 VAULT_DIR → SSE 广播变更                          │
│  └── 生产模式托管 dist/ 静态文件                                      │
└───────────────────────────────┬────────────────────────────────────┘
                                ▼
                     本地文件夹 vault（纯 .md，任何编辑器可直接打开）
```

- **部署形态**：MarkGraph 是**服务器端应用**，浏览器只做客户端。本机开发用 `npm run dev`（Vite + Fastify）；生产用 `npm run build && npm start`，默认监听 `127.0.0.1`。未来演进：多端原生 App 作为同一服务端 API 的客户端。
- **环境变量**：`HOST`（默认 127.0.0.1；反向代理后可设 0.0.0.0，无鉴权，勿暴露公网）、`PORT`（默认 7710）、`VAULT_DIR`（笔记库文件夹，仓库外，不入 git）

## 2. 技术选型与理由

| 层 | 选择 | 理由 |
|---|---|---|
| 前端框架 | React 18 + TypeScript + Vite | CM6 生态封装（@uiw/react-codemirror）成熟；TS 保证索引/状态代码可维护 |
| 样式 | Tailwind CSS + shadcn/ui | 组件复制进仓库非黑盒依赖；命令面板/菜单/对话框用 Radix 底座获得键盘导航与无障碍；主题经 CSS 变量 token 全量可控 |
| 编辑器 | CodeMirror 6 | Markdown 生态成熟，live preview 与 wikilink decoration 可自行扩展 |
| 图谱 | d3-force + 原生 canvas | 力导向布局标准库；canvas 渲染保证千节点流畅；不引图表库 |
| 状态 | zustand | 轻、无样板代码，适合中型单页应用 |
| 后端 | Fastify + tsx | SSE / 静态托管 / 路由一体，开发时用 tsx 热重载 |
| 文件监听 | chokidar | 跨平台文件变更监听事实标准 |

**明确不引入**：MUI/Antd（强视觉组件库与多主题方案冲突）、Electron/Tauri（目标是网页版）、数据库（纯文件存储）。

## 3. 目录结构

```
MarkGraph/
├── docs/                      # 本文档集
├── style-samples/             # 风格小样（独立静态页，决策用，保留作主题预览）
├── sample-vault/              # 示例库源文件（~25 篇互链中文笔记）
├── index.html
├── vite.config.ts             # dev 代理 /api → localhost:7710
├── src/
│   ├── main.tsx / App.tsx     # 入口与布局壳
│   ├── styles/
│   │   └── app.css            # 五套主题 token + 布局与组件样式
│   ├── api/
│   │   ├── client.ts          # fetch 封装（tree/read/write/create/rename/delete/events）
│   │   └── types.ts           # VaultNode / NoteContent / ApiEvent 等
│   ├── editor/
│   │   ├── Editor.tsx         # CM6 封装：live preview、模式切换
│   │   ├── wikilink.ts        # wikilink/tag 语法解析与 CM6 decoration
│   │   └── completions.ts     # [[ fuzzy 补全 source
│   ├── graph/
│   │   ├── indexer.ts         # 链接索引：nodes/edges/backlinks/tags（纯函数，输入全部笔记文本）
│   │   └── GraphView.tsx      # d3-force + canvas 组件
│   ├── panels/
│   │   ├── FileTree.tsx
│   │   └── Backlinks.tsx      # 反链 + 大纲（右栏插槽面板）
│   ├── shell/
│   │   ├── Tabs.tsx
│   │   ├── CommandPalette.tsx
│   │   ├── commands.ts        # 命令注册表
│   │   └── bus.ts             # 事件总线
│   └── state/store.ts         # zustand：tree/openTabs/activeTab/index/theme
├── server/
│   ├── index.ts               # Fastify 装配：API + SSE + 静态托管
│   ├── fs-vault.ts            # vault 读写与路径安全
│   ├── watch.ts               # chokidar → SSE
│   └── ai/                    # Phase 2：AI 富集服务层（见 §9）
│       ├── config.ts          # 环境变量与目录常量（.markgraph/）
│       ├── settings.ts        # AI 开关设置（settings.json 读写）
│       ├── openai.ts          # OpenAI 兼容客户端（chat / embeddings）
│       ├── frontmatter.ts     # frontmatter 解析与 tags/summary 写回
│       ├── store.ts           # 向量缓存、富集状态、备份与撤销记录
│       ├── links.ts           # 建议链接定位与插入（目标解析、anchor 行匹配）
│       ├── enrich.ts          # 富集流水线：防抖队列 + 编排 + AI 事件
│       └── routes.ts          # /api/ai/* 路由（薄层，只做参数校验与转调）
└── package.json               # 单包：scripts.dev = concurrently vite + server
```

## 4. API 契约

所有响应 JSON；错误统一 `{ error: string }` + 4xx/5xx。路径参数为 vault 内相对 POSIX 路径（如 `笔记/项目/读书.md`）。

| 方法 | 路径 | 请求 | 成功响应 | 说明 |
|---|---|---|---|---|
| GET | `/api/tree` | — | `{ tree: VaultNode }` | VaultNode = `{ type:"dir"\|"file", name, path, children? }`，按名称排序，隐藏项已滤除 |
| GET | `/api/note?path=` | — | `{ content: string, mtime: number }` | 仅接受 `.md` |
| GET | `/api/notes` | — | `{ notes: { path, content }[] }` | 全部笔记内容（前端索引器全量构建；只读） |
| PUT | `/api/note` | `{ path, content }` | `{ mtime: number }` | 保存笔记 |
| POST | `/api/note/create` | `{ path, isDir? }` | `{ path }` | 新建笔记或文件夹；重名返回 409 |
| POST | `/api/note/rename` | `{ from, to }` | `{ path }` | 移动/重命名 |
| POST | `/api/note/delete` | `{ path }` | `{ ok: true }` | 递归删除（前端负责二次确认） |
| GET | `/api/events` | — | SSE `text/event-stream` | 事件 `change`/`add`/`unlink`（data `{ path, kind }`）与 `ai`（data 为 AiEvent：note/progress/settings） |

**安全规则**：所有 path 经 `path.resolve(vaultDir, p)`，结果必须以 `vaultDir + sep` 开头，否则 400；写操作仅允许 `.md` 后缀（rename 除外，允许目录）。

## 4.5 AI 接口（Phase 2，`/api/ai/*`）

| 方法 | 路径 | 请求 | 成功响应 | 说明 |
|---|---|---|---|---|
| GET | `/api/ai/status` | — | `{ configured, chatModel, embedModel, running, queued, settings }` | AI 未配置时 configured=false，其余端点 503 |
| GET | `/api/ai/notes?path=` | — | `{ summary, tags, related, suggestions, enrichedAt, indexed, canUndo }` | 单篇 AI 视图（智能面板数据源） |
| POST | `/api/ai/enrich` | `{ path }` | `{ ok: true }` | 立即触发单篇富集（进队列） |
| POST | `/api/ai/enrich-all` | — | `{ ok: true, total }` | 全库富集（串行队列 + progress 事件） |
| POST | `/api/ai/apply` | `{ path, target, anchor }` | `{ ok: true }` | 应用一条建议链接（anchor 行末插入） |
| POST | `/api/ai/undo` | `{ path }` | `{ ok: true }` | 撤销该笔记最近一次 AI 写入；人工已改动则 409 |
| GET/PUT | `/api/ai/settings` | AiSettings | AiSettings | autoTags / autoSummary / autoLinks / maxAutoLinks |
| POST | `/api/ai/search` | `{ q }` | `{ results: { path, score }[] }` | 语义搜索（query 向量 → 余弦 top 8） |
| POST | `/api/ai/ask` | `{ q, history? }` | SSE `meta`(来源) / `delta`(答案增量) / `error` | RAG 问答（Phase 3，流式） |

## 5. 前端数据流与索引

1. 启动：`GET /api/tree` → store.tree；订阅 SSE
2. 打开笔记：`GET /api/note` → store.openTabs；编辑防抖 1.5s → `PUT /api/note` → 事件总线 `note:save`
3. **索引器（indexer.ts）**：输入「全部笔记路径+内容」的 Map，输出：
   - `nodes: { path, name, folder }[]`
   - `edges: { source, target, resolved }[]`（resolved=目标存在；断链目标生成幽灵节点）
   - `backlinks: Map<path, { from, context }[]>`（context=引用行去 markdown 语法后的摘录）
   - `tags: Map<tag, paths[]>`
   - 解析正则与 CM6 高亮共用 `wikilink.ts` 中同一套规则，保证一致
4. 索引重建时机：启动后首次全量；之后任一 `note:save`/`tree:change` 事件触发增量重算（全量重算在 500 篇规模下 <100ms，MVP 直接全量，不做脏标记优化）
5. 图谱从索引构图；反链面板、`[[` 补全（候选=nodes）、断链检测（edges.resolved）同源取数

## 6. 主题 token 体系

- `src/styles/app.css` 用 `[data-theme="…"]` 定义五套 CSS 变量（内部 id：`apple` / `paper` / `obsidian` / `x` / `meta`，对外显示名：玻璃 / 纸感 / 经典深色 / 纯黑 / 卡片）
- `<html data-theme="apple|paper|obsidian|x|meta">` 切换；默认 `apple`
- 组件样式只引用变量，禁止硬编码颜色（保证 F6.3 单一 DOM 多主题）

## 7. 构建与脚本

```jsonc
{
  "dev": "concurrently \"vite\" \"tsx watch server/index.ts\"",   // 开发
  "build": "vite build && tsc -p tsconfig.server.json",           // 前端产物 + server 编译
  "start": "node server/dist/index.js",                           // 生产
  "typecheck": "tsc --noEmit"
}
```

## 8. git

- 提交信息中文，格式「做了什么 + 为什么」
- `.gitignore`：`node_modules/` `dist/` `server/dist/` `.env`（vault 数据在仓库外，天然不入库）

## 9. AI 富集服务层（Phase 2）

数据流：前端 `PUT /api/note` 保存 → `enrich.ts` 防抖 8s 入串行队列 → 单次富集 = 1 次 embeddings + 1 次 chat（摘要/标签/建议链接一并产出）→ 写 frontmatter（tags/summary）与可选自动插链 → chokidar `change` → SSE → 前端干净状态静默刷新（复用 Phase 1 外部修改机制）。

关键设计：

- **不做回环**：富集只由用户保存与手动命令触发，不监听文件变更事件，AI 写入不会再触发富集。
- **正文 hash 是幂等键**：跳过判断、向量缓存、undo 校验都基于「去 frontmatter 的正文 hash」；AI 插链后新正文 hash 记入状态，向量直接复用（一次链接不改语义）。
- **乐观并发**：AI 写盘前重读文件比对富集基线内容，不一致即放弃本次写入，避免覆盖期间的人工编辑（F9.5）。
- **候选排序**：建议链接的候选笔记先按 embedding 余弦相似度取 top 20，再交给 LLM 精选，控制 prompt 体积与误链率。
- **存储**（全部在 `VAULT_DIR/.markgraph/`，chokidar 忽略点开头目录，不触发 SSE）：`cache.json`（hash→向量）、`notes.json`（path→富集状态与建议）、`settings.json`、`backups/`（AI 写入前的完整快照，保留最近 50 份）。
- **降级**：`.env` 未配 AI 时全部端点返回 configured=false，写作流完全不受影响；AI 请求失败只记日志 + 面板提示。

### AI 事件（SSE `event: ai`）

| type | 载荷 | 时机 |
|---|---|---|
| `note` | `{ path }` | 单篇富集完成（前端刷新智能面板） |
| `progress` | `{ done, total }` | 全库富集进度 |
| `settings` | `{ settings }` | 设置被修改 |

## 10. RAG 问答（Phase 3）

```
ChatView（问答标签页，本地会话状态）
   │ POST /api/ai/ask { q, history }
   ▼
routes(ask) → rag.askVault ──① embed(问题) → 每篇取最佳块（块向量余弦）top 6
   │                          ② chatStream(系统提示 + 块上下文 + 历史) 
   ▼                          ③ SSE meta(来源)/delta(增量) 推给前端
OpenAI 兼容网关（newAPI 等）
```

- **切块**：富集时把正文按「当前标题 + 空行分段」切成目标 ≤600 字的块（单段超长硬切），块文本带标题前缀一起向量化；向量存 `cache.json`（键 `${正文hash}#${块序}`），块文本存 `chunks.json`。
- **笔记级向量 = 块向量均值**：相关笔记 / 语义搜索继续用笔记级向量，检索问答用块级向量，一次 embedding 调用两用。
- **引用**：上下文按 `【笔记 笔记名】(路径)` 标注，系统提示要求答案用 `[[笔记名]]` 引用；前端渲染与阅读模式同管线（转义 → wikilink span → marked），可点击跳转。
- **会话**：多轮历史由浏览器持有随请求携带（服务端无状态），关闭标签即丢弃。
